const express =  require('express');
const app = express();

import { Request, Response } from "express";

// PRODUCTOS
app.get('/products', () => {})

app.get('/products/id', () => {})

// PEDIDOS


// FACTURAS


// PROVEEDORES